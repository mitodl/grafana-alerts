namespace: integration-testing
groups:
    - name: consul
      rules:
        - alert: ConsulUp
          expr: |-
            consul_up != 1
          for: 1m
          labels:
            severity: critical
          annotations:
            message: Consul '{{ $labels.job }}' is not up.
